apiVersion: v1
kind: Secret
metadata:
  name: nlp-api-secrets
  namespace: ml-pipelines
type: Opaque
stringData:
  # Токен для Telegram-вебхуков
  TG_BOT_TOKEN: "123456789:AAH_tvoje_token_zdes"
  
  # Ключи для моделей и БД
  HUGGINGFACE_TOKEN: "hf_tvoj_token"
  QDRANT_API_KEY: "qdrant_super_secret_key"
  WANDB_API_KEY: "wandb_key_dlja_obuchenija"

---
# Секрет специально для Qdrant (если мы обращаемся к нему из Airflow напрямую)
apiVersion: v1
kind: Secret
metadata:
  name: qdrant-secrets
  namespace: ml-pipelines
type: Opaque
stringData:
  api-key: "qdrant_super_secret_key"