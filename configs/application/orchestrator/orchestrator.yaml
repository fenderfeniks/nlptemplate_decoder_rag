_target_: src.application.orchestrator.RAGOrchestrator

# Шаблон промпта из PromptManager
default_template: "rag_qa"

# Число документов для ретривала
default_top_k: ${rag_pipeline.inference.top_k}

# Лимит сообщений в истории диалога для защиты контекстного окна LLM
max_history_msgs: 10