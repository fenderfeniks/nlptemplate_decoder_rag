# training/scheduler/cosine_warmup.yaml
# num_training_steps вычисляется программно в OptimizerMixin
# через trainer.estimated_stepping_batches — не хардкорим здесь
_target_: transformers.get_cosine_schedule_with_warmup
_partial_: true
num_warmup_steps: ${training.warmup_steps}