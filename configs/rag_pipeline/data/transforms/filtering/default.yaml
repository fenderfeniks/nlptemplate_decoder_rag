_target_: src.rag_pipeline.core.data.transforms.filtering.LengthFilterTransform
max_length: ${rag_pipeline.data.max_length}
# indexing-режим: фильтруем по input_ids.
# contrastive-режим: меняйте на query_input_ids через override в конфиге задачи.
column: "input_ids"
num_proc: ${rag_pipeline.data.num_proc}