_target_: src.pipelines.base.core.data.transforms.filtering.LengthFilterTransform
max_length: ${decoder_pipeline.data.max_length}
# indexing-режим: фильтруем по input_ids.
# contrastive-режим: меняйте на query_input_ids через override в конфиге задачи.
column: "input_ids"
num_proc: ${decoder_pipeline.data.num_proc}