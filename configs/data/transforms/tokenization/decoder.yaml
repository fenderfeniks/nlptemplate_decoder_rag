_target_: src.pipelines.decoder.core.data.transforms.tokenization.TokenizationTransform
task: ${data.task}
prompt_column: ${data.prompt_column}
target_column: ${data.target_column}
messages_column: ${data.messages_column}
max_length: ${data.max_length}
separator: ${data.separator}
num_proc: ${system.num_proc}
batch_size: ${system.batch_size}
writer_batch_size: ${system.writer_batch_size}