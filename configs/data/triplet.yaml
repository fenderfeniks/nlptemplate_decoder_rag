# configs/data/triplet.yaml
defaults:
  - source: hf_dataset

# Указываем новые колонки!
anchor_column: "query"
positive_column: "good_doc"
negative_column: "bad_doc"

collator:
  _target_: src.core.data.collators.TripletTextCollator
  max_length: 512
  anchor_column: ${data.anchor_column}
  positive_column: ${data.positive_column}
  negative_column: ${data.negative_column}
  # Гидра подкинет tokenizer автоматически из train.py!