_target_: src.pipelines.base.core.data.fetcher.RawDataFetcher
source_type: hf
raw_dir: ${system.paths.raw_data_dir}
dataset_name: databricks/databricks-dolly-15k   # обязательно: "org/dataset-name"
token: null         # HF token для приватных датасетов
# Дополнительные kwargs пробрасываются в load_dataset:
# split: train
# name: subset_name